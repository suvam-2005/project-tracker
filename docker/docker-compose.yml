services:
  postgres:
    image: postgres:16-alpine
    container_name: project-tracker-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME:-project_tracker}
      POSTGRES_USER: ${DB_USER:-tracker_user}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:${DB_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-tracker_user} -d ${DB_NAME:-project_tracker}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    name: project_tracker_postgres_data
